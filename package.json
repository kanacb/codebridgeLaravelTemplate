{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "serve": "php artisan serve --host 0.0.0.0 --port 8000",
        "prod": "npm run production",
        "ws": "php artisan websockets:serve",
        "clear": "php artisan optimize:clear",
        "i" : "composer install",
        "l" : "php artisan app:launcher",
        "m" : "php artisan migrate",
        "launch" : "npm run i && npm run l && npm run m && npm run serve"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "laravel-vite-plugin": "^0.8.0",
        "vite": "^4.0.0"
    }
}
